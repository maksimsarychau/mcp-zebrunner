{
  "name": "mcp-zebrunner",
  "version": "3.0.3",
  "private": true,
  "type": "module",
  "description": "MCP server (stdio) for Zebrunner TCM Public API (read-only)",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "node scripts/increment-version.js && tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "smoke": "tsx src/smoke.ts",
    "test": "tsx tests/test-runner.ts",
    "test:unit": "tsx tests/test-runner.ts unit",
    "test:integration": "tsx tests/test-runner.ts integration",
    "test:e2e": "tsx tests/test-runner.ts e2e",
    "test:hierarchy": "node --test tests/integration/suite-hierarchy.test.ts",
    "test:watch": "tsx tests/test-runner.ts -- --watch",
    "test:health": "tsx tests/test-runner.ts -- --health",
    "test:verbose": "tsx tests/test-runner.ts -- --verbose",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist",
    "dev:debug": "DEBUG=true tsx watch src/server.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "axios": "^1.7.3",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.17.0",
    "typescript": "^5.6.3"
  }
}
